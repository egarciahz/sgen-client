import React from 'react';
import { SelectProps, fieldToSelect } from 'formik-material-ui';
import { ErrorMessage } from 'formik';
import FormHelperText from '@material-ui/core/FormHelperText';

import AsyncSelect, { DEFAULT_CURSOR_VALUE, FetchOptions } from '../../components/AsyncSelect';
import { wrapFetchHook } from '../../utils/wrapFetchHook';

function ProjectSelectorComponent() {
    return null;
}

export default function ProjectSelector() {
    return <ProjectSelectorComponent />;
}
